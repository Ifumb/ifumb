# dependencies
node_modules/

# pnpm is the only package manager: no other lockfile is ever committed
package-lock.json
yarn.lock
bun.lockb

# next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# tests
coverage/
playwright-report/
test-results/
blob-report/

# prisma generated client (regenerated by `prisma generate` on postinstall)
src/infrastructure/persistence/prisma/generated/

# env files (secrets never committed; .env.example is)
.env
.env.*
!.env.example

# misc
.DS_Store
.idea/
.vscode/
